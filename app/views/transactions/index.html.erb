<div>
  <h1 class="font-bold text-4xl">Transactions#index</h1>
  <%= turbo_stream_from "transactions" %>
  <div id="transactions">
    <% @transactions.each do |transaction| %>
      <% render transaction %>
    <% end %>
  </div>
</div>
